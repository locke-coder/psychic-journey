raw_dashboard_autoload_path = "D:/path/to/raw_dashboard.htm"

# Durable operational data is stored only in this separate private repository.
# Prefer these generic names. Existing GITHUB_OPERATOR_SAMPLE_* settings remain
# supported for backward compatibility.
PRIVATE_DATA_REPO = "owner/private-data-repo"
PRIVATE_DATA_TOKEN = "replace_me"
PRIVATE_DATA_BRANCH = "main"
PRIVATE_DATA_PREFIX = "operator_samples"
PRIVATE_DATA_TIMEOUT_SECONDS = 10
PRIVATE_DATA_MODE = "private"

[general]
example_key = "replace_me"

[app]
access_password_placeholder = "replace_me"
