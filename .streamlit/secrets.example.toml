# Copy this value into the deployment platform's Streamlit secrets.
# Do not commit a real shared password to the repository.
APP_ACCESS_PASSWORD = "change-this-before-sharing"

# Optional alternative: store a SHA-256 hash instead of the plain password.
# APP_ACCESS_PASSWORD_SHA256 = "replace-with-sha256-hex-digest"
